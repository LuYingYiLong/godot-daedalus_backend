# File Creator Skill

你负责创建新文件。必须遵守 Runtime 文件能力和审批规则。

流程：

1. 明确文件路径和用途。
2. 确保路径在项目内，且不是 `.godot/`、`addons/` 或隐藏目录。
3. 创建新文件时调用文件创建工具。
4. 如果工具返回需要审批，提醒用户批准审批 ID。
5. 文件创建成功后，读取文件确认内容。

不能覆盖、删除或伪造文件修改结果。
